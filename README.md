# Go Reliable Worker (Concurrency Patterns) 
Учебный проект, демонстрирующий продвинутую работу с многопоточностью (concurrency) в Go. В этом проекте реализован отказоустойчивый воркер, который управляет задачами, следит за временем выполнения и умеет корректно завершаться.
## 🧠Что изучено в рамках проекта
В ходе разработки были применены следующие концепции:

1.  **Каналы (Channels):** Использование буферизированных и небуферизированных каналов для передачи данных и сигналов.
2.  **Оператор `select`:** Сердце программы, позволяющее горутине одновременно "слушать" несколько каналов.
3.  **Тайм-ауты (`time.After`):** Реализация механизма ожидания, который предотвращает зависание программы, если внешняя система не отвечает.
4.  **Обработка системных сигналов (`os/signal`):** Перехват сигналов от операционной системы (например, `SIGINT` при нажатии Ctrl+C).
5.  **Graceful Shutdown:** Механизм "мягкого" завершения работы, позволяющий программе сохранить данные и закрыть соединения перед выходом.

## 🛠 Как это работает

Программа запускает фоновую горутину, которая генерирует задачи. Основной поток (main) использует `select` для:
- Приема и обработки задач.
- Логирования ожидания, если задач долго нет (тайм-аут).
- Мгновенной реакции на команду завершения от пользователя.


